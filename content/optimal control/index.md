These notes, which fall under "optimal control", are really an amalgamation of guided readings on dynamic programming, reinforcement learning, and its connections to control theory.  

`Overlearning` is a more esoteric analysis paper on error bounds for MFNN-parametrized optimal control feedback loops;. The applications are not apparent yet (fitting a NN per time step doesn't scale), though it is a worthwhile result that can be further investigated.

